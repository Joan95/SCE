<div class="main">
	<div class="container">
		<h1>These are our Packs!</h1>
		
		<p><%= link_to "Return to Home Page", root_path %></p>

		<%= form_tag(searchPack_path, :controller => "items", :action => :searchPack, :method => :get) do %>
			<%= text_field_tag :search, params[:search] %>
			<%= submit_tag("Search Pack") %>
		<% end %>

		<p>Displaying Pack's List: <%= @itemsPacks.length %> </p>

			<% if user_signed_in? %>
				<% @itemsPacks.each do |item| %>
					<div class = "Item">
						<%= image_tag item.image %>
						<h3> <%= item.name %> - <%= item.price %>€ </h3>
						<p> Description: <%= item.description %> </p>
						<p> Category: <%= item.category %> </p>
				<%= link_to "Add to Cart", {:controller => "cart", :action => :addItem, :id => item.id }, {:method => :post} %></p>
					</div>
				<% end %>

			<% else %> 

				<% @itemsPacks.each do |item| %>
					<div class = "Item">
						<%= image_tag item.image %>
						<h3> <%= item.name %> - <%= item.price %>€ </h3>
						<p> Description: <%= item.description %> </p>
						<p> Category: <%= item.category %> </p>
					</div>
				<% end %>
			<% end %>

			
	</div>
</div>
